import { browser } from "$app/environment";
import { goto } from "$app/navigation";
import { auth } from "$lib/helpers/store";

export function load({ params }) {
    // auth.subscribe(({ loggedIn, cred }) => {
    //     if (loggedIn && browser) {
    //         console.log(loggedIn)
            
    //     }
    // });
}